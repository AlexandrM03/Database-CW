export interface OrderInfo {
    id: number;
    startPoint: string;
    endPoint: string;
    price: number;
    startDate: string;
    endDate: string;
    status: string;
    username: string;
    driver: string;
}